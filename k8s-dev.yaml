# PaymentsAPI — manifests do ambiente dev (namespace fcg-dev, criado pela plataforma — Orchestration).
# Deploy: kubectl apply -f k8s-dev.yaml -n fcg-dev
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: payments-api-sa
  namespace: fcg-dev
  # Learner Lab: sem IRSA (enable_irsa=false) — pods herdam as permissões do node role (LabRole).
automountServiceAccountToken: false
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: payments-api-config
  namespace: fcg-dev
data:
  ASPNETCORE_ENVIRONMENT: "Development"
  Jwt__Issuer: "FiapCloudGames"
  Jwt__Audience: "FiapCloudGames"
  AWS__Region: "us-east-1"
  Messaging__Scope: "fcg-dev"
---
# PaymentsAPI só valida JWT: recebe a chave PÚBLICA do secret criado pelo UsersAPI (nunca a privada).
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: payments-api-secrets
  namespace: fcg-dev
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: fcg-secretstore
    kind: ClusterSecretStore
  target:
    name: payments-api-secrets
    creationPolicy: Owner
  data:
    - secretKey: Jwt__RsaPublicKey
      remoteRef:
        key: fcg/dev/common/jwt
        property: Jwt__RsaPublicKey
    - secretKey: ConnectionStrings__Payments
      remoteRef:
        key: fcg/dev/payments/db
        property: ConnectionStrings__Payments
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: payments-api
  namespace: fcg-dev
  labels:
    app: payments-api
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app: payments-api
  template:
    metadata:
      labels:
        app: payments-api
    spec:
      serviceAccountName: payments-api-sa
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1654
        runAsGroup: 1654
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: payments-api
          # Substituir 667079134782; CI/CD (spec 09) faz `kubectl set image` com a tag da build.
          image: 667079134782.dkr.ecr.us-east-1.amazonaws.com/payments-api:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "750m"
          envFrom:
            - configMapRef:
                name: payments-api-config
            - secretRef:
                name: payments-api-secrets
            # ⚠️ Learner Lab: o SDK da AWS via IMDS (node role/LabRole) trava por causa do hop
            # limit de metadata dos nós (mesmo problema do ESO — ver Orchestration/k8s-dev.yaml).
            # Fallback: credenciais temporárias do lab como env vars. Secret populado
            # imperativamente, NUNCA commitado — recriar a cada sessão:
            # `kubectl create secret generic aws-lab-creds -n fcg-dev ...`.
            - secretRef:
                name: aws-lab-creds
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 15
            failureThreshold: 3
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: payments-api
  namespace: fcg-dev
  labels:
    app: payments-api
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-type: "nlb"
    service.beta.kubernetes.io/aws-load-balancer-internal: "true"
spec:
  type: LoadBalancer
  selector:
    app: payments-api
  ports:
    - port: 8080
      targetPort: 8080
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: payments-api
  namespace: fcg-dev
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: payments-api
  minReplicas: 1
  maxReplicas: 3
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-payments-api-ingress
  namespace: fcg-dev
spec:
  podSelector:
    matchLabels:
      app: payments-api
  policyTypes:
    - Ingress
  ingress:
    - ports:
        - port: 8080
          protocol: TCP
